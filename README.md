# dep-update-checker

Check for outdated dependencies and available updates.

## Installation

```bash
cd dep-update-checker
npm install
```

## Usage

```bash
node src/cli.js check --path /path/to/projects
```

## Features

- Checks for outdated packages
- Categorizes updates (major, minor, patch)
- Shows latest available versions
